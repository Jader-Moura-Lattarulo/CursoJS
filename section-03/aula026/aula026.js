//For In
// Diferente do for classico que conta os indices o for in lê os indices ou chaves
const frutas = ['Abacaxi', 'Melancia', 'Melão']; //Array ou vetor

for (let i in frutas) {
    console.log(i);
}