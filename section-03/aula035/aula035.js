//return - retorna um valor
//       - termina a função

function soma(a, b) {
    return a + b; //retorno da função
}

let resultadoSoma = soma(4,5);

console.log(resultadoSoma);

function soma2(a , b) {
    console.log(a + b); // está apenas exibindo na tela, mas não está retornando nada.
}

soma2(4, 5);